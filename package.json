{
  "name": "votatis",
  "version": "0.0.0",
  "private": true,
  "description": "Votatis 모노레포 — 검증 가능한 제보 아카이브",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "deploy": "pnpm --filter votatis-intake-api deploy"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["workerd", "esbuild", "sharp"]
  }
}
